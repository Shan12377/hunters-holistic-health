// Single source of truth for daily goals (CLAUDE.md Rule C).
// Every display AND every scoring calculation must import these.
// Never hardcode these numbers anywhere else.

export const STEPS_GOAL = 8000
export const WATER_GOAL_OZ = 64
export const ENERGY_SCALE_MAX = 10
